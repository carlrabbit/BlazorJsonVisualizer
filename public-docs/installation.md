# Installation

## Status

Preview / planned for first public package release.

This surface is not release-ready. It will describe supported installation flows once NuGet packaging is active.

## Planned NuGet installation

NuGet installation instructions and finalized package identifiers will be added when public package maturity changes from preview/planned to active publication.

## Repository development setup

For repository development, use the canonical engineering commands from `README.md` and `docs/engineering/command-contract.md`.

The EF Core storage backend currently exists as repository source projects:

- `BlazorJsonVisualizer.Storage.EFCore`
- `BlazorJsonVisualizer.Storage.EFCore.SqlServer`

These are not yet documented as finalized NuGet package names.
