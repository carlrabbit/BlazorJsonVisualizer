{
  "name": "@blazor-json-visualizer/runtime-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && esbuild index.ts --bundle --format=esm --platform=browser --outfile=dist/index.js"
  }
}
