# Browser Runtime

The browser runtime is a first-class subsystem responsible for runtime behavior in the browser, including rendering coordination, document navigation, and host communication boundaries.
