# Runtime Boundaries

The browser runtime and runtime core own JSON viewing, navigation, rendering coordination, and editing behavior. The Blazor host configures and communicates with the runtime but does not own runtime internals.
