# Runtime Protocol

## Purpose

Define the message and DTO contract between the Blazor host and the browser runtime.

## Initial direction

- The protocol must use explicit DTOs and predictable message shapes.
- Host-to-runtime and runtime-to-host messages must be versionable.
- The protocol must be suitable for fast contract testing.
